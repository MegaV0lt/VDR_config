# Parameters for VDR plugin actuator

# You may use some mute-script for muting the S/P-DIF out of the
# sound card even if not used for AC3.
MUTE_SCRIPT="/usr/bin/vdr_mute.sh"

#INIT_ACTION=""
#EXIT_ACTION=""
#RESTART_ACTION=""
#RECORD_ACTION=""

PLUGIN_NAME="bitstreamout"
if [ "$MUTE_SCRIPT" != "" -a -e $MUTE_SCRIPT ] ; then
   PLUGIN_PARAMETERS="--m $MUTE_SCRIPT"
else
   PLUGIN_PARAMETERS=""
fi

VDR_PARAMETERS=""
