# Parameters for VDR plugin dvbapi

#INIT_ACTION=""
#EXIT_ACTION=""
#RESTART_ACTION=""
#RECORD_ACTION=""

PLUGIN_NAME=dvbapi
PLUGIN_PARAMETERS="-d"  # Disable CW expiration check

VDR_PARAMETERS=""

# OSCam läuft auf dem Server im lokalen Netz (IdeaCentre-200)
#if [ "$(grep "dvbapi.OSCamNetwork = 1" /etc/vdr/setup.conf)" = "" ] ; then
#   [ "$(pidof oscam)" == "" ] && /etc/init.d/oscam start
#   for i in $(seq 1 10) ; do
#      [ "$(pidof oscam)" != "" ] && break
#      sleep 1
#   done
#fi
